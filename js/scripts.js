function convertLitres(number) {
  return number * 1000;
}

function convertKgs(number) {
  return number * 1000;
}

function convertTablespoon(number) {
  return number / 3;
}

function convertTempFahrenheit(number) {
  return (number * 9 / 5) + 32 + " " + "degrees fahrenheit";
}